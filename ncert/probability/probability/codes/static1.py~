#from PyDictionary import PyDictionary as pd
from collections import defaultdict


coloumn = int(input("enter the no of coloumn"))
raw = int(input("enter the no of of raw"))
table = defaultdict(dict)
for i in range(raw):
	key1 = input("enter the raw key")
	for j in range(coloumn):
		key2 = input("enter the keycoloumn")
		values = int(input())
		table[key1][key2]= values

print(table)
